- controllare addServer quando il parametro ip non è corretto - si impalla
- Controllare la PULL, quando il client non ha la repository in locale, si impalla il Loop delle richieste di          creazione. Se la repository esiste invia correttamente la struttura al server.
- // RISOLTO controllare stampa listserver per rimuovere \n
- // RISOLTO controllare list new repos quando non ci sono new repos 
- // RISOLTO help accosato a sinistra
- su tutti i comandi quando c'è un parametro mancante non deve crashare
- da gestire vuoi creare?(Y/N) - N
- quando si fa add repository e si da alla repo un repoName diverso da quello della cartella della repo l add va a buon fine mentre poi la push da errore (bisogna avvisare l utente di chiamare la repo nello stesso modo oppure gestire la cosa)
- quando una push va a buon fine non c'è una conferma che sia andata a buon fine per l utente ma solo un messaggio successo la pushrequest è stata inviata; ci si aspetta di ricevere un messaggio tipo pushrequest effettuata con successo che però non arriva mai
